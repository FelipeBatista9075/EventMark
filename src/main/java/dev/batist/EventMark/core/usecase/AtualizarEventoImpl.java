package dev.batist.EventMark.core.usecase;

import dev.batist.EventMark.core.entities.Evento;

public class AtualizarEventoImpl implements AtualizarEvento{

    @Override
    public Evento execute(Long id, Evento evento) {
        return null;
    }
}
